# Architecture Decision Records

Each ADR captures one decision, the context that forced it, and the consequences
accepted alongside it. ADRs are immutable references: they are amended in place with
a dated note rather than rewritten, and they are **never renumbered**, because their
numbers are cited from the Blueprint, the CHANGELOG and commit messages.

## Index

| ADR | Decision | Status | Implemented? |
|---|---|---|---|
| [002](ADR-002-spring-boot-bootstrap.md) | Spring Boot Walking Skeleton | Accepted (framework version superseded 2026-07-27) | Yes |
| [003](ADR-003-github-mcp-integration.md) | GitHub MCP integration | Accepted (amended 2026-07-28) | Yes |
| [004](ADR-004-api-contract-first.md) | API contract-first with OpenAPI 3.1 | Accepted (amended 2026-08-19) | Yes — `docs/api/openapi.yml` hand-authored before the first controller |
| [005](ADR-005-error-handling-strategy.md) | Error handling via RFC 9457 Problem Details | Accepted | Yes — `GlobalExceptionHandler` maps every 4xx and 5xx |
| [006](ADR-006-build-quality-gates.md) | Build quality gates | Accepted (amended 2026-08-18, 2026-08-19, 2026-08-20, 2026-08-23, 2026-08-24, 2026-09-04) | Yes — Checkstyle, SpotBugs, JaCoCo and the Javadoc gate all run in `verify`. **Amended 2026-09-04 (EOP-125):** a tenth documentation-integrity gate, `RuleVersionLiteralTest`, and the first to read `.opencode/rules/` — a version literal there must cite an ADR on the same line, because a rule file carries no `**Status:**` and no `**Date:**` and so cannot be superseded. Two declared self-retiring exceptions in `versioning.md`; the recognised shapes are a closed, measured enumeration (three-component semver and `image: name:tag`), so a two-component pin is deliberately not caught — a broad sweep returned five false positives (an IP address, two coverage percentages, three k6 rates) for every genuine pin. It proves a number has a dated owner, never that the number is right. |
| [007](ADR-007-versioning-strategy.md) | Semantic Versioning | Accepted | Yes |
| [008](ADR-008-database-migration-liquibase.md) | Database migrations with Liquibase | Accepted (amended 2026-08-10, 2026-08-19, 2026-08-23, 2026-08-23) | Yes — autoconfiguration fixed and the first changeset applies; the H2 console consequence is withdrawn and the Context labels row is glossed by ADR-043; XML over formatted-SQL rationale added (EOP-162), then its schema-validation, "silently" and file-forking claims narrowed and rollback trust grounded in executing the inverse (EOP-165) |
| [009](ADR-009-frontend-react-typescript.md) | React + TypeScript + Vite + GOV.UK Frontend | Accepted (amended 2026-08-19, 2026-08-20, 2026-09-04) | Yes — `ui/` scaffolded, built and served |
| [010](ADR-010-continuous-flow-over-sprints.md) | Continuous flow over sprint timeboxes | Accepted | Yes |
| [011](ADR-011-graphify-knowledge-graph.md) | Graphify knowledge graph via repo-local MCP server | Accepted | Yes |
| [012](ADR-012-deployment-target.md) | Deployment to a single EC2 instance with Terraform | Accepted (deployment target withdrawn 2026-08-10) | Partly — image and Compose run locally; Terraform validates but no `apply` has run |
| [013](ADR-013-feature-flags.md) | Feature flags via Spring configuration properties | Accepted | Yes — `eop.features.session-lifecycle` withholds `SessionController` and, since EOP-190 (2026-09-04), `SessionStreamController` via `@ConditionalOnProperty`; `SessionControllerDisabledIntegrationTest` asserts both beans are absent and all five routes 404. The route count is unchanged at five — EOP-190 moved the SSE route into a second controller to bring `SessionController`'s constructor under the `java:S107` parameter threshold, and repeated the condition on the new bean rather than letting it inherit one. `eop.features.trick-play` does the same for `TrickController`, `ScoreController` and `EndSessionController`, and `TrickPlayDisabledIntegrationTest` asserts its seven use-case beans and all three controllers absent — ten beans in all — as well as all seven trick-play routes answering 404 (re-derived 2026-08-21, EOP-49; this row said six use-case beans, two controllers, eight beans and six routes until then, and unlike ADR-013's own paragraph it was never dated, so it read as current while going stale — the flag's live value and arity are ADR-013's to state, and regeneration commands live there). **Both flags are fail-closed as of EOP-48 (2026-08-14, commit `34d30d7`) and were not before:** the register's two 2026-08-14 notes record that a `@ConditionalOnProperty` with no `havingValue` reduces to `!"false".equalsIgnoreCase(value)`, so `off`, `no`, `0`, `disabled` and the **empty string** all enabled the feature; `SessionController` carried that form *and* left its four session use cases registered unconditionally, so the off position was a property of the URL space and not of the context. Both halves are closed — `havingValue = "true"` at `SessionController.java:69` (anchor: `havingValue`) plus the same condition on `createSessionUseCase`, `joinSessionUseCase`, `getSessionStateUseCase` and `startSessionUseCase` (`UseCaseConfiguration.java:122` (anchor: `session-lifecycle`), `:145`, `:189`, `:204`) — and `SessionLifecycleOffValueIntegrationTest` pins the `off` spelling with 12 tests that failed 6 of 12 against the pre-fix tree, `POST /api/v1/sessions` among them answering 201 Created. `resolvePlayerUseCase` stays ungated on purpose (pure read, shared with both flags; same precedent as `DeckShuffler`), and the ADR now mandates the explicit form **and** bean-level gating repository-wide |
| [014](ADR-014-realtime-transport.md) | Real-time transport via server-sent events | Accepted | Yes — `SseSessionEventPublisher` emits `player-joined` and `game-started`, and as of EOP-14 Slice E `hand-dealt`, `card-played` and `trick-resolved` — the latter three from `HandDealer` and `TrickJournal` since EOP-190 (2026-09-04) moved each publisher next to the write it accompanies, and from `DealHandsUseCase`, `PlayCardUseCase` and `ResolveTrickUseCase` before that — each published after its write returns so a refused operation announces nothing; events carry no state. **The through-Caddy verification is a dated manual observation, not a check the build repeats.** On 2026-08-05, under EOP-10 (commit `87d8c49`), the stream was read on a real socket through Caddy with `retry:3000` and `:heartbeat` frames observed — covering the two events that existed then, `player-joined` and `game-started`, and no others. The three EOP-14 Slice E events were folded into this sentence on 2026-08-14 and were **never** part of that observation. Nothing reproduces it: `SessionStreamIntegrationTest` exercises the stream against the application directly, and ADR-035 records that no test in the suite goes through Caddy (the k6 smoke test of ADR-017 does, but not on the SSE path). Treat the frame-level behaviour of the proxy as observed once, on two of the five events — re-observe it or script it rather than citing this row as evidence |
| [015](ADR-015-player-identity.md) | Player identity via a server-issued opaque token in session storage | Accepted | Mostly — the server half is done: 43-character token issued on admission, only its SHA-256 digest stored, no response or event carries the digest, and a missing credential is refused identically to a wrong one. EOP-11 added the client half: custody in `sessionStorage` under `eop_session`, replay via the `X-EoP-Player-Token` header, and `fetch`-based SSE reading that closes this ADR's `EventSource` open question without resorting to a query parameter. One gap remains — the client's recovery from an expired or deleted session matches on the strings `403`/`404` in a problem `detail` that never contains them, so a stale token wedges the tab instead of returning it home (EOP-11) |
| [016](ADR-016-local-container-runtime.md) | Colima as the local container runtime | Accepted (amended 2026-08-23) | Yes — installed, stack runs locally, all verification gates executed. **2026-08-23 amendment (EOP-154):** the k6 → InfluxDB → Grafana pipeline that the original Neutral consequence disclosed as broken is repaired, and one of the three causes it stated was misdiagnosed — InfluxDB authentication was working correctly, and k6's output URL simply carried no credentials, so every write was an unauthenticated one. Two further read-path faults, neither previously recorded, had to be fixed before the dashboard could render anything: the provisioned Grafana datasource reused the host-facing `${INFLUXDB_URL}`, which is unreachable from inside Grafana's own container under `access: proxy`, and it declared no `uid` while all ten dashboard panels reference `uid: InfluxDB`. HTTP authentication is now disabled on a stack bound to `127.0.0.1` holding nothing but latency samples, rather than embedding a credential in a URL `test/k6/run.sh` echoes to the terminal. Verified by querying InfluxDB and Grafana's datasource proxy, never by k6's exit code, which is `0` even when every write fails |
| [017](ADR-017-frontend-delivery-topology.md) | Front-end delivery via Caddy on a single origin | Accepted | Yes — proxy serves the site and forwards the API |
| [018](ADR-018-uuid-v7-identifiers.md) | UUID v7 primary keys generated through an application port | Accepted (narrowed 2026-08-12 — see ADR-023) | Yes — `IdentifierGenerator` mints identifiers in the use case, not at flush; `UuidV7IdentifierGeneratorTest` pins the version nibble, the variant and time ordering across 2,000 draws. **Narrowed to entity rows:** `trick_play_component` is keyed on `(trick_play_id, ordinal)` and `hand_card` on `(hand_id, card_id)`, and neither carries a UUID, because their rows are a value in a bounded list and a membership of a set rather than entities. Both tables exist in changeset `004` |
| [019](ADR-019-session-lifecycle-and-join-codes.md) | Session lifecycle, join codes, and header-only authentication on the event stream | Accepted (next-player formula amended 2026-08-12 — see ADR-023; join code widened to eight characters 2026-08-22 — see EOP-24) | Yes — implemented across EOP-10: Crockford base32 codes, seat contention settled by `uq_player_session_seat`, throttled guessing, and one indistinguishable refusal for every unusable code. **Its next-player formula is superseded:** "leader's seat plus the number of cards already in the trick" holds only while every seat still holds a card, which EOP-14's whole-deck deal breaks on the last trick at the three table sizes whose deal is uneven (68 cards do not divide evenly at 3, 5 or 6 players, though they do at 4). The general form is the next seat clockwise that still holds a card. **Deck size further amended 2026-08-18 (EOP-75):** the deck is now 68 cards — see [ADR-041](ADR-041-printed-deck-has-no-aces.md). **Join code widened 2026-08-22 (EOP-24):** codes are eight Crockford characters, exactly forty bits, because thirty bits was enumerable by a distributed attacker in days rather than the centuries the original comment claimed — the limiter is now one of two controls rather than the primary one |
| [020](ADR-020-session-concurrency-control.md) | Concurrency control by compare-and-set on `status`, not by optimistic locking | Accepted (lock ordering decided 2026-08-12, corrected from a chain to a tree 2026-08-13 — see ADR-023; narrowed twice on 2026-08-14 by EOP-14 Slice E, which gave `current_leader_seat IS NULL` a third meaning and `sessionMoved` a sixth answer) | Yes — `touchWhileInStatus` and `advanceStatus` are status-guarded conditional updates whose rows-affected count is the protocol; `@Version` is mapped but deliberately unenforced, and no `OptimisticLockingFailureException` handler exists anywhere |
| [021](ADR-021-trusted-proxy-forwarded-for.md) | `X-Forwarded-For` trusted only from an explicit, default-denied allow-list of proxies | Accepted | Yes — `eop.web.trusted-proxies` defaults to the empty list, `ClientAddressResolver` ignores the header unless the peer is allow-listed, a malformed or non-canonical entry fails startup, and `ForwardedForThrottleBypassIntegrationTest` asserts both halves: the rotation attack is throttled, and the header is honoured when the peer is configured. **Amended 2026-08-22 (EOP-24):** the ADR-019 passage quoted in that file is now historical - the join code is eight characters and forty bits, and ADR-019 no longer calls this limiter a *primary* control; nothing in the reasoning is withdrawn, and the defect fixed was that the limiter keyed on a caller-supplied address, which is the half widening the code cannot help with. |
| [022](ADR-022-agent-model-tier-governance.md) | Model tiers allocated by Definition-of-Done role, not by artefact type | Accepted (rule 1 superseded 2026-08-21 by ADR-046; amended 2026-08-24 by ADR-059) | Yes — `@tester-api` pinned to `{env:MODEL_B}`, every DoD gate on a reasoning tier, and each carries a Sign-off Contract in its own definition. This cell read "all five DoD gates on `MODEL_A`/`MODEL_B`" until 2026-08-21, when ADR-046 moved `@architecture-guardian` and `@security-auditor` to `MODEL_F` and replaced rule 1's tier-name whitelist with a capability floor plus a mandatory probe; rules 2 to 6 stand. The narrowing of the Separation Invariant is recorded rather than denied, and rule 5's second exception — production code the primary agent authors itself instead of delegating — is now closed, leaving one: test code. The diagram in the Decision section is the 2026-08-11 arrangement, retained as history. `AUDITOR_AGENTS`/`READ_ONLY_AGENTS` in `tools/agent-trace.py` are split so the write check no longer false-alarms on a tester writing a test |
| [023](ADR-023-deal-remainder-and-turn-order.md) | Every card is dealt with the remainder going to seats in ascending order, the current leader's seat stored on `game_session`, and `TrickPlay.components` stored one row per component in `trick_play_component` | Accepted (decision 1 superseded 2026-08-17 by EOP-72, restored 2026-08-18 by EOP-92) | **Decision 1 reads correctly again as of EOP-92 (2026-08-18), after a spell in which this summary was stale.** EOP-72 replaced the full deal with equal hands and a discarded remainder, and this row was not amended to say so — it went on describing the whole-deck deal for one story while the code discarded two to four cards per game. EOP-92 reverts EOP-72 at the product owner's request: `Hands.deal` deals every card again, so 68 cards go out as 23/23/22, 17/17/17/17, 14/14/14/13/13 or 12/12/11/11/11/11 in seat order with nothing discarded, `Hands.openingLeaderSeat` is total by construction rather than by a swap that always landed the lead on the last seat, and the short final trick is exercised rather than merely supported at 3, 5 and 6 players (four players is the exception — 68 divides evenly by four, so hands are equal there). Conservation is pinned by `HandsTest.shouldDiscardNothingAtAnyTableSize` across all four table sizes; this also resolves divergence 1 of EOP-83 in favour of `docs/api/openapi.yml`, which had documented the full deal all along. Partly — the domain half of decision 1 shipped in EOP-14 Slice A: `Hands.deal` round-robins the whole deck so the remainder falls on the lowest seats, `Hands.openingLeaderSeat` derives the opening lead with no rank written down, and `Trick.nextLeaderSeat(seatsHoldingCards)` passes the lead on when the winner has run out of cards. Decision 2 is **built and now reachable** as of EOP-14 Slice C1: `current_leader_seat` landed in Liquibase changeset `004`, and `HandRepository.findCurrentLeaderSeat`, `HandRepository.recordDeal` and `TrickRepository.recordResolution` now read and advance it through `TrickPlayRepositoryAdapter`; Slice C2 now calls them from `DealHandsUseCase`, `PlayCardUseCase` and `ResolveTrickUseCase`, which derive the leader seat from a read and never from a request body; and as of EOP-14 Slice D `TrickController` reaches all four of those beans over HTTP, gated on the same flag, with Slice E adding a fifth, `GetTrickStateUseCase`, behind a fifth route on the same controller and the same flag. EOP-14 Slice E discharges the last of the deferral: `ResolveTrickUseCase` passes `Trick.nextLeaderSeat(seatsHoldingCards)` straight through to `TrickRepository.recordResolution`, whose next-leader parameter widened from `int` to `OptionalInt`, so a played-out hand records `current_leader_seat = NULL` rather than a winning seat with nothing left to lead, and `GET /api/v1/sessions/{sessionId}/tricks/current` publishes `seatToPlay`, `complete`, `nextLeaderSeat` and `handComplete` so a client can learn whose turn it is without attempting a play and being refused. The components decision, added by the 2026-08-12 amendment, is likewise **built and now mapped**: the domain layer of its 20-element bound shipped in Slice A, `trick_play_component` with `PRIMARY KEY (trick_play_id, ordinal)` and `CHECK (ordinal <= 19)` ships in changeset `004` with migration tests pinning the CHECK to `TrickPlay.MAX_COMPONENTS`, and the `@Size(max = 20)` boundary layer shipped in Slice D at `PlayCardRequest.java:53-54`, pinned to `TrickPlay.MAX_COMPONENTS` rather than to a literal, with a per-element bound and an HTTP test for both sides of it; `TrickPlayComponentJpaEntity` maps the table as of Slice C1, and the adapter writes ordinals it computes itself rather than trusting a payload. Slice B also creates `hand`, `hand_card`, `trick`, `trick_play` and `trick_play_component` with 6 unique constraints and 10 foreign keys; `hand_card` keyed on `(hand_id, card_id)` is what makes the `20, 20, 19, 19` deal storable at all and was missing from this ADR's own changeset inventory until the 2026-08-12 amendment. **Seat binding *is* in storage**, reversing an earlier deferral on the same day: `uq_player_id_seat` on `player (id, seat_order)` plus `fk_hand_player_seat` and `fk_trick_play_player_seat` make a hand or a play at a seat its player does not hold unrepresentable, and the single-column keys to `player(id)` are subsumed rather than omitted. **Cross-session enforcement remains deliberately not in storage**, and Slice C2 did not put it there — it ships no Liquibase changeset, so as far as the database is concerned the play path, the deal path (`hand` has the identical gap) and the delete path all still accept a row naming a player, a seat or a card that belongs to another session. What Slice C2 did instead is make its own three paths unable to *construct* such a row: every identifier a write carries is derived from a read scoped to the session the caller was resolved in — `PlayCardUseCase` takes the acting seat and player from the resolved player and the hand from that session's `Hands`, and `DealHandsUseCase` deals only to the seats the resolved session reports — so a cross-session play is refused above storage rather than by it. That is narrower than the obligation and should be read as narrower: it protects the three paths this slice wrote, not the five tables, and the first future path that reaches these ports without repeating the discipline is unguarded again. Audit then found a **fourth** gap that is not cross-session at all: `fk_trick_winner_play` confines a trick's winner to no trick and no session. The ADR names **four** Slice C obligations, each naming its exception type and the HTTP status the refusal carries — 403/404 for the seat and membership checks, 409 for the duplicate constraints, 422 for the wrong-trick winner, and a fourth obligation to extend the adapters' constraint-name translation so those violations stop answering 500 (@tester-api, 2026-08-12). **Obligation 4 shipped in Slice C1** for every row of its table except the two seat foreign keys, which the adapter deliberately treats as backstops and rethrows as server faults because it refuses a wrong seat *above* the constraint; that row is superseded in place. **Obligation 1 is partly discharged, and not in the layer it names**: `assertSeated` throws both `NotYourSeatException` and `PlayerNotInSessionException` with the body parity the obligation requires, but that is a check on the *row* — does the player a row names hold the seat it claims — and not on the requester, so deriving the acting player from authenticated identity and refusing a caller-supplied seat are discharged by Slice C2, whose three use cases resolve the caller first and derive the acting seat from the resolved player, and whose `PlayCardCommand` cannot carry a seat at all. Changeset `005` added range CHECKs to `player.seat_order`, `trick.sequence` and `game_session.current_leader_seat`, pinned to `GameSession.MAXIMUM_PLAYERS`. Slice C1 also **mapped all five tables** — `@Table` now appears on eight classes, not three — so they are reachable through `HandRepository` and `TrickRepository` by any bean that injects either port, and so is every deferral above. Containment is no longer the absence of a use case, because Slice C2 wrote the callers: what contained the deferrals at Slice D was `eop.features.trick-play`, in front of six use-case beans and both `TrickController` and `ScoreController`; Slice D wrote the route and gated it on that same flag with `havingValue = "true"`, and an off-position test asserted the beans absent and the routes answering 404. A flag is weaker than a missing class, because it is one property away from live rather than one commit — **and that property was changed: EOP-70 flipped the flag `true` on 2026-08-17, so nothing on this list is contained any longer.** Every deferral above is reachable through a live route, and only the authorisation in [ADR-024](ADR-024-trick-play-persistence-boundary.md) stands in front of it (noted 2026-08-21, EOP-49; the sentences before this one are left as they were written, with their tense pinned, because the argument still holds — but the arity has grown past what they say and the current figures are [ADR-013](ADR-013-feature-flags.md)'s to state, dated, rather than a fifth place to drift). See [ADR-024](ADR-024-trick-play-persistence-boundary.md) for the boundary those two ports draw, and the reversal and the remaining deferrals in the ADR |
| [024](ADR-024-trick-play-persistence-boundary.md) | One adapter implements both trick-play ports, and neither port authorises anybody | Accepted | Yes — `TrickPlayRepositoryAdapter` implements `HandRepository` and `TrickRepository` as one class in EOP-14 Slice C1, because playing a card deletes a `hand_card` row and inserts a `trick_play` row with its components and those writes must commit together; the ports stay separate because their callers are separate. Neither port takes an acting player, so no implementation of them can authorise a requester: `recordDeal` and `appendPlay` check the row, `openTrick` and `recordResolution` check nothing, and the four reads have no parameter to check against. Authorising the requester is the use case's obligation and is **discharged as of EOP-14 Slice C2**: `DealHandsUseCase`, `PlayCardUseCase` and `ResolveTrickUseCase` each resolve the caller through `ResolvePlayerUseCase` as their first port call — the ordinal was "first statement" until EOP-49 re-derived it and found a null check and a session-id read ahead of the call in `PlayCardUseCase`, which changes nothing about the property, since authorisation still precedes every hand, trick and card read — and `PlayCardCommand` carries no seat, no player identifier, no suit and no rank, so a caller cannot name a seat at all. No port can enforce this and no C1 test failed for its absence, which is why it was recorded here rather than only in javadoc; what holds it now is a test per use case asserting that a stranger is refused **before any hand is read**. The oracle described above is therefore reachable only by a member — and reachable in earnest, not latently: `eop.features.trick-play` was `false` when this row was written, gating the beans out of the context, but EOP-70 flipped it `true` on 2026-08-17, so the gate is open and only the authorisation above stands between a requester and the oracle (noted 2026-08-21, EOP-49; the flag's current value and arity live in [ADR-013](ADR-013-feature-flags.md)) **Amended 2026-08-13 (EOP-14 Slice C2):** the second Consequences bullet is retracted - a `@Transactional` composer in the outer ring can demarcate one unit of work, because Spring's default `REQUIRED` propagation makes two `@Transactional` adapter methods join an enclosing transaction, so the use-case package was never the only place left to declare it. |
| [025](ADR-025-dealing-is-its-own-use-case.md) | Dealing is its own use case rather than a step in starting a session, and the deal follows the start write across a seam the ADR argues is chosen rather than forced | Accepted (decided 2026-08-13; both handoffs it deferred discharged 2026-08-14 — Slice D minted the three event names, Slice E published them and deleted the `nextLeaderSeat` placeholder) | Yes — EOP-14 Slice C2 ships `DealHandsUseCase`, `PlayCardUseCase` and `ResolveTrickUseCase` as plain classes in `org.maglez.eop.usecase`, wired in `UseCaseConfiguration` behind `@ConditionalOnProperty` on `eop.features.trick-play`, which `application.yml` defaulted to false when this row was written; `StartSessionUseCase` is untouched and still deals nothing. Authorisation is the first port call of all three, `DealHandsUseCase` returns `void` and refuses a participant with `NotFacilitatorException` and a short table with `TooFewPlayersException` before the deck is even read, and `ResolveTrickUseCase` admits any seated member rather than the facilitator alone. The `DeckShuffler` port with `SecureRandomDeckShuffler` supplies the shuffle `Hands.deal` deliberately omits, and `CardRepository.findWholeDeck()` returns the whole deck in canonical order rather than a page. `TrickPlayDisabledIntegrationTest` asserts all three beans are absent with the flag off while the ungated shuffler remains (noted 2026-08-21, EOP-49: the flag is `true` — EOP-70 set it on 2026-08-17 — so these beans exist and their routes are served; that test now covers ten beans over seven routes, and the live value and arity are [ADR-013](ADR-013-feature-flags.md)'s to state, dated). Two things were deliberately not in Slice C2: no route reached these beans, so the started-but-undealt window of decision 2 was not yet recoverable by a client, and recognising the end of a hand — where `ResolveTrickUseCase` currently writes the winning seat as a placeholder next leader — is Slice E's. EOP-14 Slice D closed the first of the two: `TrickController` now reaches all three beans plus the eleventh use case `ReadOwnHandUseCase`, so a facilitator can deal and a player can read their own hand. The second is closed by EOP-14 Slice E: the placeholder is gone, `TrickRepository.recordResolution` takes an `OptionalInt` next leader and records no leading seat once no seat holds a card, `HandCompleteException` refuses a play into a spent hand with its own 409 rather than the 422 an empty hand would otherwise produce, and `handComplete` on the new state-of-play read says the hand is over without claiming the score is final |
| [026](ADR-026-use-case-observability.md) | Where use case observability lives — option 4 chosen (EOP-70): audit logging at the HTTP boundary in `TrickController` | Accepted (2026-08-17) | Yes — EOP-70 discharged this ADR by choosing option 4: audit logging at the HTTP boundary in `TrickController`. Each of the three game-affecting write endpoints emits one `INFO` line naming the session UUID and the first 8 characters of the actor's token; card identities are never logged (CWE-117 and hidden-information constraints). Two use cases (`ResolveTrickUseCase`, `SweepExpiredSessionsUseCase`) carry pre-existing SLF4J imports from earlier stories — accepted debt. This choice is adequate while every use case is reached through HTTP. |
| [027](ADR-027-singleton-subresource-naming.md) | A singleton sub-resource may be singular, and `/hands` is forbidden rather than merely unused | Accepted | Yes — EOP-14 Slice D ships `GET /api/v1/sessions/{sessionId}/hand`, singular, departing from `api-design.md`'s binding plural-noun rule because the path resolves its subject entirely from the identity token and takes no selector, so it is a singleton by construction rather than a collection that happens to hold one member. The substantive half of the decision is the prohibition, not the naming: no route, use case, port method or DTO may return a hand the caller does not hold, so there is no `HandsDto`, no `HandPage` and no `/hands`. Nothing mechanically enforces that — the way to enforce "this route must never exist" is a review — but adding one would need a new port method or use case, both conspicuous in a diff. `api-design.md` is unamended and plural remains the default; this is a named exception with a stated test. Recorded because @architecture-guardian ruled during the Slice D review that a javadoc is invisible to the next agent, who reads the rule file rather than the controller. **Amended 2026-08-21 (EOP-49):** the ADR-013 bullet's "withheld by `eop.features.trick-play`, answers 404" is no longer live - EOP-70 set the flag `true` on 2026-08-17, so the route is served and only ADR-015's token and ADR-024's authorisation stand in front of it; the bullet is pinned in place rather than superseded because it sits outside any dated block. |
| [028](ADR-028-end-of-hand-without-release-or-score.md) | The end of a hand is reported, not released and not scored | Accepted (amended 2026-08-15 — automatic `COMPLETED` transition added by EOP-15 Slice C) | Yes. **Amended 2026-08-17 (EOP-70, ADR-040):** all three predecessors are discharged - EOP-48, EOP-15 via ADR-032, and ADR-026's option 4 - and `eop.features.trick-play` is now `true` in `application.yml`; the flag and its guards remain until the feature is confirmed stable.
| [029](ADR-029-global-rule-loading.md) | All rule files loaded globally via a single glob | Accepted | Yes — `.opencode/opencode.json` `instructions` is `[".opencode/rules/*.md"]`; nine `## Required Reading` sections deleted from agent definitions (EOP-000) — EOP-14 Slice E reports the end of a hand in two derived places and stores it in neither: `current_leader_seat` becomes `NULL` once no seat holds a card, and `handComplete` on `GET /api/v1/sessions/{sessionId}/tricks/current` is computed from `Hands.allEmpty()`. The session status transitions to `COMPLETED` automatically when the last trick resolves (EOP-15 Slice C, ADR-032), or earlier if the facilitator calls `POST /api/v1/sessions/{sessionId}/end`. The second decision is a deliberate non-change: `eop.features.trick-play` stayed `false` on merge, so all five slices shipped unreleased and turning the flag on was its own story whose predecessors were [ADR-026](ADR-026-use-case-observability.md), EOP-48 and EOP-15 — that story ran: EOP-70 flipped the flag `true` on 2026-08-17, all three predecessors are discharged, and the five slices are released (noted 2026-08-21, EOP-49; the flag's live value is [ADR-013](ADR-013-feature-flags.md)'s to state) |
| [030](ADR-030-scoring-is-derived-not-accumulated.md) | The score is derived from play, not accumulated alongside it | Accepted | Yes — EOP-15 Slice A ships `ScoredPlay`, `Standing` and `ScoreSheet` as pure domain types with zero framework imports, and `ScoreSheet.of(players, tricks)` recomputes every figure from the plays and each trick’s winning play rather than reading a stored total. There is no counter column, no increment on the play path and no schema change in this slice, because a stored total is a second authority that can disagree with the play that produced it and the disagreement is silent. That is also the fuller reading of the ticket’s "computed and stored server-side": what it protects is authority, and a derivation leaves the client nothing to assert against. Six further decisions ride with it — the shipped rule stays one point for a threat on your own card plus one for the trick and is explicitly not embellished, with the author’s abandoned v0.21 system and the whitepaper’s beginner relaxation recorded as options rather than built; a linked threat that names no component still scores, settling the question `TrickPlay` left open, because the application holds no model of the system and so cannot validate a component name, and making a point turn on unvalidated client free text would return the score to the client by a side door; a row’s `threatPoint` and `trickPoint` stay separate rather than pre-summed so a human reading the Score Card can see why a row scored; ties are shown by competition ranking and never broken, and no tie-break is invented to get wrong later; a display name is never a key, since two players may choose the same one (ADR-015); and an unresolved trick contributes threat points but no trick point, which makes a running score monotone and lets one class serve both the mid-game read and the final standings. Deciding a game has ended is deliberately not this class’s job — the `COMPLETED` transition is the session’s and arrives in the story’s third slice. **Amended 2026-08-21 (EOP-49):** the merge-safety bullet's "which it stays" is spent - EOP-70 set the flag `true` on 2026-08-17, so what this slice added is reachable now; the bullet stands as the merge-safety record because the reasoning was sound, and only the containment in front of it has gone. |
| [031](ADR-031-the-score-is-read-through-its-own-route.md) | The score is read through its own route, and a contradicted game is a server fault | Accepted | Yes — `GET /api/v1/sessions/{sessionId}/score` is served by `ScoreController` behind `eop.features.trick-play`, hand-authored into `docs/api/openapi.yml` before the controller existed per ADR-004. `GetScoreUseCase` takes two collaborators, `ResolvePlayerUseCase` and `TrickRepository`: resolving the caller already yields the session and its players, so no second read of the session is needed, and because a token is only ever matched against the players of the session it was presented for, membership is established by that resolution alone and no further seat check is required. The one new port method, `TrickRepository.findTricks(sessionId)`, returns the session's whole history and filters nothing — an unresolved trick comes back unresolved, because whether a trick is finished is a question the trick answers about itself, and its plays have already scored their threats. It takes no acting player, as ADR-024 requires. Its adapter reads four queries for the whole session rather than three per trick, which at three players is four round trips instead of `1 + 3 × 23` = seventy. There is deliberately no `409`: before the deal the honest answer is everybody on nothing, so unlike the state-of-play read this route needs neither `HandRepository` nor a status check. ADR-027's prohibition is answered in writing rather than assumed — it forbids exposing every player's *private* state through one route, and a score names only cards already face up, which is what separates it from `/hand`. Slice A's eight `IllegalArgumentException` refusals became one `ScoreNotDerivableException` carrying a typed `Reason`, mapped to 500 with a body byte-identical to the no-tampering-card fault, so no identifier can be echoed into a Problem Details response; the four remaining guards interpolate only bounded integers and stay `IllegalArgumentException`. 989 tests (962 before), SpotBugs 0, Checkstyle 0, JaCoCo met over 119 production classes (112 before) with all seven new types at 100% instruction coverage and no branches, and ADR-006's reserved 70% branch minimum restored in `pom.xml` while the bundle measures 90.19%. The flag stayed `false` while these slices shipped and slice C owned the `COMPLETED` transition; both are spent — slice C closed the transition and EOP-70 flipped the flag `true` on 2026-08-17, so `/score` is reachable (noted 2026-08-21, EOP-49; the flag's live value is [ADR-013](ADR-013-feature-flags.md)'s to state). |
| [032](ADR-032-end-of-game-transitions.md) | End-of-game transitions | Accepted | Yes — `GameSession.complete()` and `SessionRepository.recordCompleted()` ship in EOP-15 Slice C; auto-complete fires in `ResolveTrickUseCase` when the last trick resolves, and `POST /api/v1/sessions/{sessionId}/end` provides facilitator early end, both gated on `eop.features.trick-play`. `GAME_COMPLETED` event added to `SessionEventType`. No new DB column — `updated_at` serves as completion time. `SessionNotInProgressException` → 409 in `GlobalExceptionHandler`. |
| [033](ADR-033-session-creation-rate-limit-and-body-size-cap.md) | Session creation rate limit and request body size cap | Accepted | Yes — `SessionCreationLimiter` port and `InMemorySessionCreationLimiter` adapter (EOP-19); reserve-before-work pattern; `RateLimitedException`; `TrustedProxyProperties.sessionCreationLimit`; Caddy `request_body { max_size 16KB }` on `/api/*`; no `max-swallow-size` — considered and deliberately not added (would misrepresent a non-existent second layer). **Amended 2026-08-22 (EOP-24):** keyspace exhaustion is no longer the operative harm - the eight-character Crockford code gives about 1.1 trillion keys, so a flood would need decades and the 503 is a remote consequence; the decision to rate-limit creation stands unchanged, on unbounded growth of `game_session` by an unauthenticated caller. |
| [034](ADR-034-sse-subscriber-cap-and-emitter-timeout.md) | SSE subscriber cap, emitter timeout, and heartbeat stall fix | Accepted (amended 2026-09-05, 2026-09-06) | Yes — per-session cap 12 (2× MAXIMUM_PLAYERS), global cap 500, 10-minute emitter timeout, elastic send thread pool (4–500 threads, 60s keepalive, SynchronousQueue, LoggingDiscardPolicy), `spring.mvc.async.request-timeout: 600000` (EOP-20, EOP-223). |
| [035](ADR-035-tls-and-security-response-headers.md) | TLS at Caddy and security response headers | Accepted | Yes — `tls internal` in the Caddyfile, host port mapping changed from `80:8080` to `443:8080`, eight security response headers (CSP, X-Content-Type-Options, Referrer-Policy, HSTS, Permissions-Policy, and — added 2026-08-20 — Cross-Origin-Opener-Policy, Cross-Origin-Resource-Policy, Cross-Origin-Embedder-Policy) applied globally, `Server` header removed, token-storage constraint formalised (EOP-21); site address widened to the loopback IP literals so they no longer bypass the header block, and `*.map` refused at the edge (EOP-107). |
| [036](ADR-036-session-expiry-and-sweep.md) | Session expiry (24-hour TTL) and abandoned-session sweep | Accepted (EOP-22, 2026-08-16) | Yes — `expires_at` column added by Liquibase changeset `006`, expiry guard in `ResolvePlayerUseCase` (ungated), `SweepExpiredSessionsUseCase` + `ExpiredSessionSweepScheduler` gated on `eop.features.session-lifecycle`. Reverses ADR-019's "no session expiry" consequence. |
| [037](ADR-037-frontend-build-time-feature-flags.md) | Front-end feature flags are build-time Vite variables, not runtime properties | Accepted (EOP-11, 2026-08-16; amended 2026-08-18 by EOP-77 and EOP-78, 2026-08-22 by EOP-145, and 2026-08-24 by EOP-168 — see the amendment paragraphs under Consequences) | Yes — there are now three live instances of this pattern. `VITE_CARD_MAGNIFIER_ENABLED` (EOP-145, 2026-08-22) gates the card magnifier lens and is read in `GameScreen.tsx`, the first flag read below the view router. `VITE_FOLLOW_SUIT_HINT_ENABLED` (EOP-168, 2026-08-24) gates the proactive follow-suit hint, is also read at component scope in `GameScreen.tsx`, and is threaded into `FollowSuitHint` as a prop so the component itself owns the gate; all three positions (on, off, unset) are tested via `vi.stubEnv` in `GameScreen.test.tsx`. Earlier flags were retired as their features stabilised (EOP-77, 2026-08-18 amended; `VITE_LOBBY_UI_ENABLED` was retired once the lobby UI was stable). `VITE_GAME_SCREEN_ENABLED` is declared in `ui/src/vite-env.d.ts` and read in `App.tsx` (the view router) as `import.meta.env.VITE_GAME_SCREEN_ENABLED === 'true'`, so an unset flag fails closed. Both positions (flag on and flag off) are tested via `vi.stubEnv` in `App.test.tsx`. |
| [038](ADR-038-pointer-event-drag-and-drop.md) | Pointer-Event drag-and-drop for card play, with no DnD library | Accepted (EOP-60, 2026-08-16) | Yes — `GameScreen.tsx` uses `onPointerDown`/`onPointerMove`/`onPointerUp`/`onPointerCancel` with `setPointerCapture` and `getBoundingClientRect` drop detection. Drag is an enhancement: every card is a `role="button"` with keyboard handler and a "Play selected card" button provides the WCAG 2.2 AA keyboard path. `VITE_GAME_SCREEN_ENABLED` declared in `vite-env.d.ts`, off by default. Per the EOP-79 amendment, no descendant of the drag surface may be natively draggable — all three card `<img>` elements (hand card, trick-zone played card, drag ghost) carry `draggable={false}`, because a native HTML5 drag fires `pointercancel` and aborts the gesture; the ghost does not strictly need it, its wrapper having `pointerEvents: 'none'`, but is guarded so the invariant holds uniformly. `handlePointerDown` also cancels the `pointerdown` default action, so a mouse press no longer moves focus. The amendment records that jsdom cannot reproduce a native drag, so the two `draggable="false"` assertions on the hand and trick-zone images are the only automated guards on that regression; the ghost is guarded but unasserted. |
| [039](ADR-039-new-game-reset.md) | New-game reset: COMPLETED → IN_PROGRESS via non-atomic multi-step sequence | Accepted (EOP-65, 2026-08-16) | Yes — `NewGameUseCase` clears tricks then hands, resets session via CAS, re-deals. `PersistGameResultUseCase` called best-effort from `ResolveTrickUseCase` via `Optional`. |
| [040](ADR-040-trick-play-flag-on.md) | Enable `eop.features.trick-play` (EOP-70) | Accepted (2026-08-17) | Yes — `trick-play: true` in `application.yml`; ADR-026 discharged (option 4: audit logging at HTTP boundary in `TrickController`); UI wires `POST /deal` after `POST /start`; `canStartGame >= 3`; `GameScreen` distinguishes 409 (HandNotDealtException) from 404 (session gone). |
| [041](ADR-041-printed-deck-has-no-aces.md) | Printed deck has no Ace cards — deck size is 68, King is highest (EOP-75) | Accepted (2026-08-18) | Yes — `Rank.ACE` removed, `Card.isOpenThreat()` removed, migration `2026-08-18--remove-ace-cards.xml` deletes 6 Ace rows. |
| [042](ADR-042-game-over-flag-on.md) | Enable `eop.features.game-over`, and the shipped-flag-default trap (EOP-82) | Accepted (2026-08-18) | Yes — `game-over: true` in `application.yml`; `GameOverController` and the three use-case beans are live, so leaderboard refusals are RFC 9457 again and `game_result` rows are written. `GameOverScreen` ejects on 403 only, with a `Retry loading results` button on 404. `ShippedFeatureFlagDefaultsTest` pinned the three shipped flag defaults by reading `application.yml` off the classpath, recorded as an interim workaround rather than the tripwire EOP-27 built; EOP-84 deleted it for the derived three-way guard in ADR-053, which keeps the classpath-read technique and drops the hand-named keys. Expiry condition for the flag is dated, not open-ended. |
| [043](ADR-043-liquibase-contexts-are-not-used.md) | Liquibase contexts are not used — a bare `context="prod"` restricts nothing unless `spring.liquibase.contexts` names a real, non-empty context in every profile, verified across 21 YAML encodings (EOP-35) | Accepted (2026-08-19) | Yes — no changeset carries a `context`, the property is unset, `.opencode/rules/database.md` keeps the directive, and `LiquibaseContextGatingAbsentTest` fails the build on either half |
| [044](ADR-044-changelog-file-naming-is-dated.md) | New changelog files are named `YYYY-MM-DD--<description>.xml` and the legacy `NNN-` sequence is frozen at `006-`, because `<includeAll>` orders by filename and a `007-` file would execute ahead of all three dated migrations (EOP-36) | Accepted (2026-08-19) | Yes — `.opencode/rules/database.md` carries the directive and the one-line reason; the ordering proof, the frozen-identity argument and the `classpath:` history live in the ADR. Enforced by review only — no test asserts the scheme, deliberately. |
| [045](ADR-045-frontend-response-validation.md) | Response DTOs are parsed at the browser boundary by hand-written per-DTO parsers, and a contract violation is a 502 `ContractViolationError` (EOP-108) | Accepted (amended 2026-09-05, previously 2026-08-19) | Yes — the ten JSON-returning helpers in `ui/src/api.ts` parse instead of asserting, the four `is*` guards from EOP-105 gain production call sites, and both fields whose contract schema `$ref`s the `StrideCategory` mirror are narrowed to it: `CardDto.suit` by EOP-108 and `TrickDto.ledSuit` by EOP-109, the latter through an `optionalEnum` helper so absence stays absence. `Card.rank`/`CardDto.rank` remain bare `string` against a `$ref` to `Rank`, deliberately — see §4. Strictness is bounded to structure and enum membership, not value formats. Parser field coverage is enforced by review only — no test detects a field added to a DTO but not to its parser. |
| [046](ADR-046-gate-model-capability-floor.md) | A Definition-of-Done gate is pinned by a capability floor and a passing probe, not by tier name (EOP-000) | Accepted (amended 2026-08-24 by ADR-059) | Yes — rule 1 of ADR-022 is superseded: a gate tier must be reasoning-capable, emit structured tool calls, carry at least 40,000 max output tokens and at least 128,000 context, be family-independent of every tier that authored the artefact it reviews, and have PASSED the two-stage probe recorded in Blueprint §3.4.1. `MODEL_F` is created as `amazon-bedrock/minimax.minimax-m2.5`, holding `@architecture-guardian` and `@security-auditor`, which closes the second exception to the Separation Invariant. The probe is mandatory because three candidates satisfying every metadata clause on paper proved unusable — one emitted an unparseable reasoning stream, one fabricated a file read, and one answered correctly without calling the tool at all. Nothing enforces any clause programmatically, so the wording is the mechanism; `MODEL_C`/`MODEL_E` stay ineligible as a consequence of the floor rather than by name |
| [047](ADR-047-h2-excluded-from-deployable-artifact.md) | H2 is excluded from the deployable artifact rather than moved to test scope, so local development keeps its in-memory database while the jar that ships carries no web console (EOP-34) | Accepted (amended 2026-08-22) | Yes — `spring-boot-maven-plugin`'s `repackage` execution excludes `com.h2database:h2`, removing `BOOT-INF/lib/h2-2.4.240.jar` and its 13 `org.h2.server.web` classes from the artifact; the 2,685,418-byte nested jar is gone. The scope stays `runtime` deliberately, because the default profile resolves `org.h2.Driver` and ADR-012 makes `./mvnw spring-boot:run` a decision rather than an accident — proven unbroken by starting it and getting a 200 from `/health`. Gated by `tools/artifact/assert-no-h2-in-jar.sh` in the `build` job, which no *surefire* test could do since surefire runs before `package` — a failsafe IT could, and was rejected only because declaring one would make a seventh declared Maven plugin; proven non-vacuous by restoring `origin/main`'s `pom.xml` and watching it fail. EOP-34's second item, a `maven-enforcer` ban on the console module, is **declined** — a ban with no sanctioned escape hatch trains people to disable bans, and the EOP-27 tripwire is the satisfiable control for the same risk. Two corrections recorded: `runtime` scope already kept `JakartaWebServlet` off the compile classpath, so the hand-registration path needs reflection or a second reviewed change rather than "zero dependency changes"; and a laptop still has H2 on its runtime classpath, so this gates the artifact and not the workstation. **2026-08-22 amendment:** its `<excludes>` block was, until EOP-38, the sole mechanism preventing a deployed artifact from serving the API schema — a load it was never designed to carry and which its own text did not claim — and a second, independent configuration-layer mechanism now exists. Its revisit trigger is not tripped. The jar-fails-on-default-profile observation was independently reproduced during EOP-38, with the exact error quoted in ADR-049 |
| [048](ADR-048-narrow-exception-handler-to-domain-type.md) | Narrow the IllegalArgumentException handler to a domain type, preserving persistence exception translation (EOP-28) | Accepted | Yes — `InvalidInputException` minted as a subclass of `IllegalArgumentException`, the handler narrowed to it, caller-facing guards converted (PageQuery, DisplayName, TrickPlay), internal guards left as JDK type and now become logged 500s, and `GlobalExceptionHandlerTest` gains a reflection test asserting no JDK type appears in any `@ExceptionHandler` except the catch-all. The subclass is load-bearing because Spring's persistence exception translation rewrites a bare `IllegalArgumentException` from a `@Repository` into `InvalidDataAccessApiUsageException`, preserving the 500-for-corrupt-row behaviour; extending `RuntimeException` would have escaped translation and echoed a database error to the caller as a 400 |
| [049](ADR-049-springdoc-disabled-by-default.md) | Springdoc disabled by default — secure configuration is the base, not the overlay | Accepted (2026-08-22) | Yes — `springdoc.api-docs.enabled: false` and `springdoc.swagger-ui.enabled: false` moved from `application-prod.yml` into `application.yml` so the default profile is secure; local dev opts in via `SPRINGDOC_APIDOCS_ENABLED` and `SPRINGDOC_SWAGGERUI_ENABLED` in `.env.example`. `application-prod.yml` keeps a redundant second guard. Measured: default profile returns 404 on `/v3/api-docs` and both Swagger paths, and 200 on `/health`; with both vars set to `true` the schema returns 200 with the exact 18,739 bytes measured before the change. The ticket's claim about `server.error.*` was disproven — Spring Boot has defaulted those to false since 2.3 and the project bypasses `BasicErrorController` via its RFC 9457 `@ControllerAdvice`, so the real fail-open surface was springdoc alone. Option B (refuse to boot insecurely) was rejected: its assertion would be dead code because the jar fails on the default profile before reaching any Spring configuration (ADR-047 excludes H2), it would need a brittle "looks like a deployment" heuristic, ADR-047 precedent favours satisfiable controls, and a plain `@SpringBootTest` is cheaper and stronger. The single-mechanism fragility is recorded — ADR-047's `<excludes>` was the only guard, and it is artifact hygiene, not profile safety. The two-profile count and ADR-012's byte-identical rationale survive untouched. |
| [050](ADR-050-dependency-cve-scanning.md) | CVE scanning for both application dependency trees as a separate Trivy CI job, gating on HIGH and CRITICAL only (EOP-89) | Accepted (2026-08-22) | Yes — `dependency-cve` job in `.github/workflows/ci.yml` runs `tools/supply-chain/scan-dependencies.sh`, which scans `pom.xml` and `ui/package-lock.json` via Trivy 0.74.0 pinned by version and SHA-256, exit contract 0 clean / 1 gating finding or allowlist drift / 2 could not run. Before this there was no CVE detection anywhere: the enforcer covers convergence and banned coordinates, and `audit-plugins.sh` covers the OpenCode developer plugins, neither of which looks at the deployed application. The first run found six real findings in compile or runtime scope, one HIGH — CVE-2026-54291 in `org.postgresql:postgresql`, a MITM protection bypass in the one directly-declared dependency of the six. All six were closed by four patch-level property overrides in `pom.xml` rather than suppressed, so the allowlist `tools/supply-chain/accepted-cves.json` ships empty and the job is green on arrival. Failure policy demonstrated end to end in six steps, reverting the real HIGH and observing exit 1, then exit 0 with a suppression, then exit 1 again on an expired suppression and once more on a suppression that had outlived its finding. Five caveats stated in the ADR and not papered over: the four version overrides are rot liabilities with no automated check, to be deleted once the Boot parent catches up; the scan reads the resolved tree rather than the shipped jar, so ADR-047's H2 exclusion is not honoured and an H2 CVE would gate falsely; scanning the fat jar was measured and does not work, which is why CycloneDX is deferred rather than rejected; the gating tier is empty today, so the policy rests on the demonstration rather than on a standing log; and the job is not a required status check when it lands, for a narrower reason than the `supply-chain` job's, with branch protection living outside the repository. **Amended 2026-09-05 (EOP-149): `dependency-cve` is now a required status check on `main`, so the last of those five caveats is spent** — the four preconditions EOP-149 set are met, the false-positive rate is measured at nought across the 60 `main` runs between 2026-08-22 and 2026-09-05, and the two reds in that window are one genuine third-party CRITICAL disclosure against embedded Tomcat, closed by upgrade under EOP-189 rather than by suppression. The escape hatch when a third-party advisory reddens an unrelated pull request is the allowlist rather than a person: upgrade first, then a time-boxed `accepted-cves.json` entry carrying a reachability trace and a mandatory `expires`, and only as a last resort an administrator lifting the requirement, recorded in the pull request. Two caveats survive the promotion — branch protection still lives in repository settings, so this row records an intent that only those settings enforce and nothing in the repository detects the drift; and the job must never acquire an `if:`, because a required check that is skipped leaves a pull request waiting on a status that never arrives. Runtime measured at 4.75s warm and about 6s more cold, cache `~/.cache/trivy`. The job also runs `setup-java` and `dependency:resolve` behind a `~/.m2/repository` cache before scanning, adding about 3s warm: Trivy's Maven parser keeps no cache of its own and re-fetches every parent POM per run, which earned a `429 Too Many Requests` from Maven Central on this job's first CI run — the exit-2 contract held and the job failed loudly rather than reporting a clean tree, and ADR-050's Consequences record the correction to its own earlier claim that no JDK was needed. |
| [051](ADR-051-read-route-rate-limit.md) | Read routes are rate limited by an MVC `HandlerInterceptor`, keyed on the resolved client address rather than on any request header or on the player token (EOP-88) | Accepted | Yes — `ReadRateLimitInterceptor`, registered by `WebInterceptorConfiguration` (the application's first `WebMvcConfigurer`) on `GET`/`HEAD` under `/api/v1/**` and excluding `/api/v1/sessions/*/events`, counts requests in a new package-private `SlidingWindowCounter` (60-second window, prune-check-insert atomically under one lock, fail-closed once the key table saturates) configured by `ReadRateLimitProperties` at `eop.web.read-rate-limit.limit: 300` and `max-tracked-keys: 10000`. The key is `ClientAddressResolver.of(request)`, which lets `ReadRateLimitInterceptorTest` assert over twelve candidate headers — including `X-Forwarded-For` rotated on every request and `X-EoP-Player-Token` — that none of them moves a caller into a fresh bucket; `server.forward-headers-strategy: none` is the second independent guard. Refusal reuses `RateLimitedException`, so the existing single `@ControllerAdvice` supplies 429, `application/problem+json` and `Retry-After` with no new exception type and no new handler — `GlobalExceptionHandlerTest` already covered that mapping before the story began. `ReadRateLimitIntegrationTest` lowers the limit to 3 for its own context with `@SpringBootTest(properties = …)` plus `@DirtiesContext` and asserts the problem+json body, that an exhausted caller receives 429 rather than 403 or 404 on `GET /api/v1/sessions/{id}/leaderboard`, and that the event stream is not counted; its isolation comes from a per-class property, never from forging the key, which would be the EOP-26 vulnerability rather than a test technique. On the client, `GameOverScreen` now caps retries at five with 1/2/4/8/16-second cooldowns and then advises reloading. Caveats the ADR records rather than hides: the counters are in memory and per-instance, so a restart forgets them and a second instance would double the effective limit; NAT'd players share one bucket and address rotation still sidesteps the window, making this a deterrent rather than an absolute cap; SSE reconnection is deliberately not limited and is a real remaining gap; the public card catalogue shares one budget with the token-bearing reads; the sliding-window algorithm now exists three times, because the creation and join limiters were deliberately left untouched rather than refactored alongside a brand-new limiter, with migration filed as a follow-up; and since the test suite raises the limit to `Integer.MAX_VALUE`, no test asserts that production runs with 300. |
| [052](ADR-052-having-value-mandate-is-build-enforced.md) | The `@ConditionalOnProperty(havingValue = "true")` mandate from ADR-013 is now enforced by a build-failing bytecode test, which also forbids `matchIfMissing` (EOP-50) | Accepted (2026-08-23) | Yes — `ConditionalOnPropertyHavingValueTest.java` reads compiled bytecode via Spring's `SimpleMetadataReaderFactory` over a `FileSystemResource` per `.class` file, walking `target/classes` with NIO `Files.walk` (zero new dependencies), enumerates every `@ConditionalOnProperty` site in `org.maglez.eop.**`, and asserts two independent invariants at each: that it carries `havingValue = "true"`, and that it does not carry `matchIfMissing = true`. The spike verified it finds exactly 21 sites (6 class-level + 15 method-level), matches a grep, and reads annotation defaults so an omitted `havingValue` reads as empty string `""` and fails the assertion — the violation is detected positively rather than by failing to find text. All 21 existing sites already comply, so the check lands green; it is **preventive**, protecting against the third recurrence. The check covers every `@ConditionalOnProperty` in the package, not only `eop.features.*` ones — infrastructure toggles like a future `eop.cache.enabled` are in scope by the same fail-closed reasoning. **The second invariant was added during EOP-50's own review round,** on @security-auditor's finding that a gate asserting `havingValue` alone would admit `matchIfMissing = true` — which satisfies ADR-013's mandate to the letter while enabling the feature whenever the property is absent, inverting the fail-closed default, and which needs no operator action at all to fire. No site sets it today, so that half is preventive on the same terms; the two invariants were empirically confirmed independent, one of four tests failing against a loosened site while the `havingValue` assertion passed untouched. ADR-013 was amended on the same day to carry the prohibition itself, after @architecture-guardian's gate called it split authority for the enforcement ADR to own an invariant the mandate ADR did not state; ADR-052 owns the mechanism only. **The bean-repetition clause of ADR-013 is not mechanically checkable:** "opens or mutates the flagged state" is a semantic judgement about domain behaviour that no annotation encodes, the carve-outs are justified in javadoc prose, and mechanising it would recreate the drift-prone second register that EOP-48 rejected. The first clause is machine-enforced, the second remains reviewer-enforced, and this asymmetry is stated openly in the ADR rather than implied to be fully automated. |
| [053](ADR-053-feature-flag-registry-build-gate.md) | The set of `eop.features` flags is derived from the shipped YAML and the compiled `@ConditionalOnProperty` sites and held in three-way agreement with a registry declaring intended state, owning story and expiry (EOP-84) | Accepted (2026-08-23) | Yes — `FeatureFlagRegistryTest.java` plus `src/test/resources/feature-flag-registry.yml`. Ten tests. Field *values* are type-checked as well as the field set: `shipped-default` must be an unquoted YAML boolean and `expiry` a quoted string, each asserted rather than coerced. Checking the field set catches a misspelled field name but never a misspelled value, and a tolerant read would turn `shipped-default: flase` into a deliberate declaration that the flag ships dark — which, if `application.yml` carried the same typo, would *agree* and pass while the flag shipped OFF, reintroducing the EOP-82 fault through the guard built to catch it. Added during this story's own gate round on @tester-api's finding. It reads `application.yml` as a `ClassPathResource` through `YamlPropertiesFactoryBean` (never the `Environment`, which every `@SpringBootTest` sees pinned ON by test resources — the masking that let EOP-82 ship a finished feature dark for two stories), walks `target/classes` with Spring's `SimpleMetadataReaderFactory` as ADR-052's scanner does, and parses the registry with SnakeYAML already on the classpath, so zero new dependencies. All three pairings fail the build: a YAML key with no entry is an undeclared flag, an annotation key in neither YAML nor registry is the fail-closed trap itself, and an entry with neither is a stale or misspelled declaration. All four registry fields are mandatory and the field set is checked in both directions, so `shipped-defualt` fails as a surplus field rather than passing as a harmless one; `expiry` accepts a quoted ISO date or the literal `null` but may not be omitted, which is what stops a new flag inheriting no-expiry by silence, and it is evaluated against `LocalDate.now(ZoneOffset.UTC)` so the verdict does not depend on which side of midnight the build ran. This replaces `ShippedFeatureFlagDefaultsTest`, deleted here, whose three hand-named keys made a fourth flag invisible — ADR-042 recorded it as an interim workaround and specified this. Five deliberate mutations were run and each failed exactly one test and no others. Three vacuity guards exist because two empty sets compare equal, and they earned their place immediately: the first implementation read the YAML via `Properties.stringPropertyNames()`, which admits a key only when its value is also a String, so Spring's parsed booleans were silently dropped and the derivation returned empty while `getProperty` kept working. Caveats stated rather than implied — `owner-story` and `shipped-default` are checked for shape and agreement, never for truth, so a plausible wrong value passes; the registry is still hand-editable, and what is removed is the silent case rather than the need for review; ADR-037's front-end `VITE_*` flags are a different mechanism and remain uncovered, so a front-end feature can still ship dark; the test-resource pin that caused EOP-82 is routed around, not removed, and EOP-27's fix for it stays unavailable; and `game-over`'s 2026-09-18 expiry will turn `main` red on a calendar boundary in a project that deploys every passing commit, which is accepted as the intended signal on the `accepted-advisories.json` precedent. |
| [054](ADR-054-openapi-contract-drift-gate.md) | The OpenAPI contract is a build gate — the generated springdoc document is compared against the hand-authored file (EOP-95) | Accepted (2026-08-23) | Yes — `OpenApiContractDriftTest.java` compares the generated `/v3/api-docs` against `docs/api/openapi.yml` on two axes: path+method operation set, and declared response status codes per operation. Four tests, bidirectional drift detection, no allow-list. Required truthful `@ApiResponse` annotations on every controller — `SessionController` had none, and every GET route was missing the 429 from ADR-051's read limiter. The gate does **not** catch prose drift (so it would not have caught EOP-72), schema drift, reachability, or proxy-layer contributions beyond the one declared 413. The header comment in `openapi.yml` is amended to record the gate. |
| [055](ADR-055-k6-performance-check-in-ci.md) | k6 performance check runs inside the `image` CI job, from a digest-pinned Docker container, with relaxed CI-specific thresholds (p95 < 500ms) that fail hard on breach but do not block merges; promotion to required status check is a manual future step (EOP-155); amended 2026-08-24 to add CI trend tracking via perf-trend job and trend page | Accepted (2026-08-23, amended 2026-08-24, 2026-09-04 and 2026-09-05) | Yes — implementation by @devops-engineer. **Amended 2026-09-05 (EOP-206):** adds `K6LatencyCheckTest` as the sixteenth documentation-integrity gate, the third to gate `test/k6/`, failing the build if a k6 `check()` predicate reads the `timings` object — the exact shape EOP-157 deleted that read as a latency gate while gating nothing. The read is bound to a `check()` block rather than banned outright, because reading `timings` for logging or a custom `Trend` is legitimate. It does not prove the remaining predicates are meaningful, does not recognise latency assertions written without `timings`, and its scanner does not handle regex literals or template interpolation — all reviewer-enforced. **Amended 2026-09-05 (EOP-156):** the canary loads `GET /api/v1/cards?size=1` as well as `GET /health`, which supersedes two coverage claims that were wider than the scenario: §3's rationale for the relaxed `p(95) < 500` threshold ("still catching gross regressions (N+1 queries, missing indexes, 50× slowdown)") and amendment 3 §6's bullet that the canary is a smoke test of `/health` alone. Two scenarios and two summary exports, not one merged run — a single `http_req_duration` would dilute a cards regression behind health's ~3× faster responses, and widening what `summary.json` measures would inject a step change into `ci-history.jsonl` caused by measurement rather than code, so the published trend stays `/health`-only. Both invocations share one `run_canary` function and one `EXPECTED_THRESHOLD_COUNT=3`, which `K6ThresholdCouplingTest` requires. Negative-tested with a stub `docker` across five modes with every fault applied only to the cards scenario, so a green `/health` cannot mask a red cards run; per this ADR's own lesson the runner path is proven only by this change's own CI run. **Amended 2026-09-04 (EOP-160):** the `--network host` grant is superseded by `--network container:eop-caddy` with `BASE_URL=https://localhost:8080`. Amendment 1's verification stands as history — what was wrong was *must*: the host grant was sufficient, not necessary, and it conferred every host port and every loopback service on the runner for a step needing outbound access to one proxy. The address must stay `localhost` because `ui/Caddyfile` serves `tls internal` for only three identifiers and a non-matching non-empty SNI aborts server-side, which is what kills both the service-name and IP-literal alternatives. Probed locally against a minimal Caddy stand-in with no published ports at all, 663/663 checks and all three CI thresholds green; the end-to-end runner path is proven only by this change's own CI run |
| [056](ADR-056-postgres-migration-tests-via-testcontainers.md) | Liquibase migration tests run against real PostgreSQL 17 through Testcontainers, scoped to `*IT` classes so the fast H2 suite stays sub-second and Docker-free (EOP-164) | Accepted (2026-08-23) | Yes — `PostgresChangelogIT`, `PostgresSchemaValidationIT` and `PostgresRollbackRoundTripIT` run the full changelog, the `@ServiceConnection` schema validation and the apply/rollback/re-apply cycle against `postgres:17-alpine` under `./mvnw verify`. Two things this does not do: it does not fix the join-code rollback defect it exposed, which stayed with EOP-163 and was pinned by three tests named `DEFECT (EOP-163)`, since inverted by ADR-057; and it adds no CI workflow step, because the `build` job already runs `./mvnw verify` on a runner with a Docker daemon. |
| [057](ADR-057-honest-join-code-rollback.md) | The join-code widening rollback refuses rather than truncating — a marker changeset dated after the widening adds and immediately drops a validated `CHECK (LENGTH(join_code) <= 6)`, so the rollback aborts while any eight-character code is live instead of rewriting it (EOP-163) | Accepted (2026-08-23) | Yes — `2026-08-23--guard-join-code-rollback.xml` applies `<empty/>` forward and carries the guard in its `<rollback>`, which `<includeAll>`'s alphabetical ordering makes Liquibase unwind first. The three `DEFECT (EOP-163)` tests in `PostgresRollbackRoundTripIT` are inverted rather than deleted, and `JoinCodeRollbackGuardTest` covers the same cases on H2 in the fast suite. Two consequences: fully unwinding the widening now needs `rollbackCount=3` rather than 2, and the guard is deliberately conservative — it also refuses the genuinely reversible padded-only case, because padding destroyed the distinction. Nothing in the build detects a future changelog dated before 2026-08-23 sorting between the widening and the guard. |
| [059](ADR-059-code-review-gate-on-its-own-model-tier.md) | The code-review gate gets its own model tier, and the Separation Invariant becomes a build gate for code (EOP-179) | Accepted (2026-08-24) | Yes — `@code-reviewer` moved off `MODEL_B` onto a seventh variable `MODEL_G` = `amazon-bedrock/zai.glm-5`, a sixth model family, cleared by a fresh code-review-specific two-stage probe with a `MODEL_B` control recorded in Blueprint §3.4.1; the 2026-08-21 verdict for the same model was explicitly not treated as transferable, because it screened for architecture and security-audit work. `SeparationInvariantTest` joins `src/test/java/org/maglez/eop/docs/` as the tenth repository-integrity gate: it resolves every agent's pin from `.opencode/opencode.json` against the active block of `.env.example`, fails loudly rather than skipping when a variable cannot be resolved, and fails `./mvnw verify` when a Definition-of-Done gate shares a model identifier with an agent that authored an artefact class that gate reviews. It was demonstrated to fire by reverting the pin (two failures, naming both tester overlaps). The invariant now holds with zero exceptions **for production code, infrastructure and test code** — it is **not** unconditional: `@security-auditor` and `@architecture-guardian` still share `MODEL_F` and the latter authors ADRs and C4 models, so architecture documentation is still reviewed by a tier-mate at one model identifier, carried as the single declared, justified, self-retiring allow-list entry. ADR-058 is left permanently unused, having been allocated to the withdrawn EOP-178 attempt |
| [060](ADR-060-sonarqube-issue-ratchet.md) | SonarQube runs locally and commits its report; CI ratchets the three Clean Code issue counts against a committed baseline with no server, gating on a freshness hash so a report can go stale but not silently (EOP-181) | Accepted (2026-08-26) | Yes — `compose.sonar.yml` runs a digest-pinned SonarQube 26.8.0 on loopback with embedded H2 and no `sudo`; `tools/sonar/scan.sh` scans locally and writes `sonar-report.json`; `tools/sonar/ratchet.sh` is the whole gate and is the same command the `sonar-ratchet` CI job runs, needing no JDK, no token and no network. The measured baseline is 11 Reliability / 232 Maintainability / 0 Security — not the 11 / 57 / 2 that prompted the story, and the ADR records the Maintainability and Security discrepancies as unexplained rather than inventing a cause; the 68%-vs-95.1% coverage gap *is* explained, by a scan following `mvn test` instead of `mvn verify`. Two deliberate deviations from the ticket: CI never writes the baseline, because a PR-triggered job pushing to a contributor branch is a security and usability hazard, so `scan.sh` tightens locally and CI reports read-only; and the files live under `tools/sonar/` per house convention rather than the `scripts/` the ticket guessed. 87% of findings are in test code, so a future coverage story will likely trip it. **Amended 2026-09-02 (EOP-000):** that prediction came true, and the answer was to narrow the gate rather than raise the ceiling or stop scanning — the three gated integers now cover `src/main/java` only and read 1 Reliability / 31 Maintainability / 0 Security. Test code is still analysed, still hashed and still recorded, under `scope.TEST` beside a new `scope.ALL` that keeps the whole-tree figure reconcilable; it simply no longer gates, so a green `sonar-ratchet` now says nothing about `src/test/java`. The reason was not coverage, which was measured at 95.1% before and after **Amended 2026-09-04 (EOP-193):** the job is a **required** status check now, and so is `sonar-ratchet-ui` — the friction the original decision wanted measured was measured and came back adverse in the opposite direction, PR #362 having merged with the ratchet red and `main` staying red across two further merges for about a day because nothing blocked. Branch protection lives in repository settings, so this row and `ci.yml` record an intent that only the repository owner can enforce and no agent can even read back (the token 403s on the protection endpoint), which means the two can drift apart silently. **Superseded in part 2026-09-05 (EOP-149):** the "what stays advisory" list in that amendment is down to `ui` and `supply-chain`, because `dependency-cve` is a required check too now — its reasoning about a CVE gate reddening on someone else's publication was sound and is still the best statement of the hazard, but the hazard had been lived through by then and the false-positive rate measured at nought across sixty runs, so which risk is worse had changed. See [ADR-050](ADR-050-dependency-cve-scanning.md) as amended. |
| [061](ADR-061-two-new-dod-gates-sonar-ratchet-and-cve.md) | The Definition of Done grows from five gates to seven — a SonarQube ratchet adjudicator and a CVE adjudicator, both on `MODEL_F` (EOP-000) | Accepted (2026-09-02) | Yes — `@sonarqube-expert` and `@dependency-vulnerability` adjudicate ADR-060's committed report/baseline pair and ADR-050's Trivy scan respectively, computing nothing themselves: both are `edit: deny`, the Sonar gate may never pass `--tighten`, and the drafted `.opencode/sonar-baseline.json` of estimated metrics was rejected outright in favour of the one measured baseline under `tools/sonar/`. The drafted zero-severity CVE policy was likewise rejected for ADR-050's HIGH/CRITICAL gate. Both pin to `{env:MODEL_F}` on fresh role-specific two-stage probes recorded in Blueprint §3.4.1, which is where the round's failures are also recorded: `MODEL_G` misreported the ratchet's exit code as 0 twice and is on record as unsuitable, `MODEL_F` failed the same role on its first attempt by inventing rule titles and dismissing two genuine findings as false positives, and the `MODEL_B` control passed but is structurally ineligible because it authors test code that this gate reviews. One round existed only because the first brief demanded a `file:line` that the four-part issue fingerprint (quality, rule key, path, content hash) does not carry. `SeparationInvariantTest` grows to seven `GATE_REVIEWS` entries with `MINIMUM_AGENTS` 17 and `MINIMUM_COMPARISONS` 27, and `ALLOWED_OVERLAPS` stays at exactly one entry because neither new agent authors anything. Two things this does not do: it does not make either CI job a required status check, and it does not narrow the surviving architecture-documentation exception. **Amended 2026-09-02 (EOP-000):** when ADR-060 narrowed the ratchet to production code the same day, `@sonarqube-expert`'s `GATE_REVIEWS` entry narrowed to `PRODUCTION_CODE` alone, taking the true comparison count from thirty to twenty-seven and `MINIMUM_COMPARISONS` from 27 to 24. The `MODEL_B`-is-structurally-ineligible argument lapsed with it — `MODEL_B` authors no production code — so the pin rests on the ADR-046 capability floor and the recorded probe instead |
| [062](ADR-062-model-a-on-the-global-inference-profile.md) | `MODEL_A` moves from the `eu.` inference profile to `global.`, because a client-side payload change made the EU profile uninvocable (EOP-000) | Accepted (2026-09-02) | Yes — OpenCode `1.18.26` sends an adaptive-thinking `block_binding.prefix_mismatch_behavior` field for every model in a hard-coded list that includes `claude-opus-5`, and `amazon-bedrock/eu.anthropic.claude-opus-5` rejects it with `Extra inputs are not permitted`, so every request on the primary tier failed. The identifier moved to `amazon-bedrock/global.anthropic.claude-opus-5` at all four sites that carry it: `.env`, `.env.example`, the Blueprint §3.4.1 live identifier table and its commented mirror. `MODEL_B` and `MODEL_D` stay on `eu.` because `claude-sonnet-4-6` and `claude-haiku-4-5` are absent from that list, and the commented Zen block is untouched because Zen names carry no region prefix. The operator confirmed EU-only inference is not a requirement here, which is what made the option available rather than blocked, so ADR-046's rejection of a `global.` profile for two audit gates stands as written and is not overturned. `SeparationInvariantTest` passes unchanged: the new value still parses, stays distinct from the other Anthropic tiers and stays in the same family, so no family boundary moves and `ALLOWED_OVERLAPS` stays at one entry. Three things this does not do: it does not make the project EU-resident (four tiers were already bare identifiers), it does not change `AWS_REGION`, which selects the endpoint a request is sent to rather than where inference is served, and it does not license a second tier moving to `global.` without its own reason |
| [063](ADR-063-sonarqube-frontend-project.md) | The front end is a second SonarQube project, `eop-threat-modeling-ui`, with its own scan script, committed report, committed baseline and CI job, rather than being folded into the existing Java project. Vitest v8 coverage is wired up at the same time, and the scanner runs in a digest-pinned container rather than as a brew-installed binary (EOP-187) | Accepted (2026-09-03) | Yes — `tools/sonar/scan-ui.sh` seeds `tools/sonar/sonar-ui-report.json`, `tools/sonar/ratchet-ui.sh` compares it against `tools/sonar/sonar-ui-baseline.json`, and the `sonar-ratchet-ui` CI job runs that comparison with no server and no network, exactly as `sonar-ratchet` does for Java. Two projects rather than one because the gate is three integers, and one set of three would let a front-end regression be paid out of Java headroom while the number still moved truthfully. The three shared Python scripts are parameterised by flavour rather than copied, and the Java branch was checked to emit byte-identical output, so the existing ceiling needed no rescan. Read the honest limits in the ADR before citing this: coverage is recorded and **not** gated at just under 91 per cent, so deleting tests passes the gate provided no new issue appears; `src/main.tsx` is deliberately left in scope reporting zero rather than excluded; front-end test files are measured and not gated for ADR-060's reason; the scanner image is amd64-only and emulated on Apple silicon, making the local scan slow; and the freshness hash closes the accident of forgetting to rescan, not the intent to hand-edit a committed report **Amended 2026-09-04 (EOP-193):** `sonar-ratchet-ui` is a **required** status check now, alongside `sonar-ratchet`, per ADR-060 as amended. The symmetry is a choice rather than a finding: the front-end project never suffered the incident that prompted the change, and was promoted with the Java job so a front-end regression cannot merge while the identical Java one is blocked. Because both are required and neither carries an `if:`, adding a path filter or event guard to either would leave every pull request permanently unmergeable. |
| [064](ADR-064-pinned-container-audit-coverage.md) | Every digest-pinned container in the repository is audited as one roster by `tools/supply-chain/audit-containers.sh` against a committed baseline, rather than the k6 image alone (EOP-159) | Accepted (2026-09-04) | Yes — `tools/supply-chain/expected-containers.json` declares all three pinned images (`grafana/k6`, `sonarqube`, `sonarsource/sonar-scanner-cli`) with seven mandatory fields each, and a new step in the existing non-required `supply-chain` job runs the audit before the Node setup, with `if: always()` added to the plugin audit so neither finding hides the other. Read the honest framing before citing it: a digest pin *is* its content, so unlike the npm audit this is **not** compromise detection — it reads no image contents, no CVE feed and no signatures. What it enforces is coverage and procedure: bidirectional roster drift (which also catches *unpinning*), mirror agreement across the three files that duplicate the `scannerImage` digest — a live hole `sonar-ratchet-ui`'s `sourceHash` does not cover — pin form, registry shape, and ADR-055 trap 2's `linux/amd64` requirement gated on whether the image is used under `.github/workflows/`. `mediaType` is compared to the baseline rather than required to be an index, because `sonarsource/sonar-scanner-cli` legitimately is not one. Sixteen negative tests all fail with an accurate first finding, including k6's real arm64 child digest paired with a fully self-consistent baseline, caught on the amd64 check alone. A registry failure or a zero-reference scan exits 2 rather than passing. The GitHub Actions layer (nine actions, fifteen unpinned sites), the Maven plugin layer (~31 parent-owned versions) and image CVE scanning are all deliberately out of scope, the last reusing the two-allowlists-diverge-silently argument |
| [065](ADR-065-agent-role-boundaries-at-the-permission-layer.md) | Agent role boundaries are enforced by each agent's frontmatter `permission` block, not by its prose; the four advisory experts hold only the read tools, the Product Owner may write only under `docs/requirements/**` and holds no `bash` beyond four read-only `git` inspections, the eleven delivery agents may not commit or publish, and the Tech Lead commits and — since the 2026-09-05 amendment — pushes a topic branch and opens its pull request, but may not merge or release (EOP-000); the 2026-09-06 amendment then removes one Jira deny from the Product Owner rather than adding a grant to it — `transition_issue` was circumventable via `update_issue`'s `fields` JSON, so it is now an honest allow while assignment, worklogs, sprints, versions and the destructive tools stay denied | Accepted (2026-09-04, amended 2026-09-05 and 2026-09-06) | Yes — all seventeen agent definitions carry a scoped `permission` block, and the three `product-owner.md` passages that contradicted one were rewritten to redirect rather than to prohibit. Written after `@product-owner` implemented a story for the second time: seven files, 560 insertions, a commit, a branch, a push and a pull request, none of it its work to do, with prose in its own brief already saying "You do not start delivery yourself." Two undocumented mechanics were probed empirically first, because the design needs both — a glob map under `edit:` does match worktree-relative paths, and a top-level `"*": deny` catch-all is honoured — and two incidental traps are recorded with them: `opencode run --agent` silently falls back to `build` when handed a `mode: subagent` agent, so the first probe measured the wrong agent, and `deny` removes a tool from the roster rather than prompting, which is what makes a denial a boundary. A fourth breach vector that was documented nowhere is closed here per agent: `run_job` takes `agent`/`prompt`/`command`/`model` overrides and `schedule_job` cron-runs an arbitrary prompt, so the mutating scheduler tools defeat `bash: deny` and `edit: deny` if left open — `@performance-engineer` keeps the job lifecycle because a scheduled job runs the k6 load tests, and the other eleven do not. Read the honest limits before citing this: the `bash` rules on the twelve delivery agents and the Tech Lead are **blocklists** and a deliberate departure from `security.md`'s preference for allow-lists, inheriting Blueprint §7.8's three ways the friction lifts, so they are a speed bump; only the flat denials and the path-scoped `edit` are hard. **Amended 2026-09-04 (EOP-000):** the agent files are walked now. `AgentPermissionDeclarationTest` parses every `.opencode/agents/*.md` frontmatter with SnakeYAML and fails `./mvnw verify` if an agent declares no `permission` block, omits `edit` or `bash` without a `"*": deny` catch-all covering it, or leaves any of the six mutating scheduler tools undenied without a declared, length-checked, self-retiring exception — `@performance-engineer` and `@tech-lead` are the only two. Closing it took eight agent files as well as a test: `architecture-guardian`, `db-designer`, `devops-engineer`, `performance-engineer`, `tester-api`, `tester-unit-and-quality`, `ui-builder` and `tech-lead` held `edit` by inheritance and now state `edit: allow` outright, which changes nothing they may do and everything about whether the grant is on the record. The gate checks *declaration*, not sense — `bash: {"*": allow}` satisfies it while granting everything — so it closes the silent case that happened twice here and leaves the judgement to review. `read` is unrestricted throughout — this is a role boundary, not a confidentiality one — and `@` invocation is unaffected |
| [066](ADR-066-jira-restored-as-the-tracker.md) | Jira is restored as the authoritative tracker and GitHub Issues becomes read-only history; the two forked keyspaces are reconciled by mirroring the fifteen GitHub-native issues into `EOP-183`–`EOP-197` with a permanent key offset, closing the seven Jira tickets whose work had shipped, and renumbering only the three keys `main` already spends on other work (EOP-000); amended 2026-09-06 to allow the Product Owner `transition_issue`, one of the seven delivery-side denies it originally listed, after that deny was found reachable around via a `status` key in `update_issue`'s `fields` JSON — the other six stand | Accepted (2026-09-05, amended 2026-09-06) | Yes — the reconciliation is done and verified: `project = EOP AND key >= EOP-166` returns 35 issues, 23 Done, 1 In Progress, 11 To Do, and Jira's next key is `EOP-201`. Seven tickets were closed against named commits rather than assertion (`EOP-166` `c91bcb6`+`26b874f`, `EOP-169` five commits, `EOP-175` `d2291a6`, `EOP-179` `f25484a`/`81e2dbe`/`8300cf6`, `EOP-180` via ADR-060–063, `EOP-181` `f20fcf5` plus four fixes, and `EOP-178` as superseded by `EOP-179` after PR #305 was withdrawn unmerged); fifteen mirrors carry their GitHub origin link and delivering commit in the description; and `EOP-167`→`EOP-198`, `EOP-168`→`EOP-199`, `EOP-182`→`EOP-200` each carry a `Duplicate` link plus a comment naming the replacement, with `EOP-200` retaining parent Epic `EOP-180`. `.opencode/agents/product-owner.md` now allows `atlassian_jira_*` and denies the delivery-side writes individually, so the agent files and refines but cannot transition, assign, log work, touch sprints or versions, or delete — and `move_*` is repeated at agent level because a per-agent block replaces the top-level one rather than merging with it. Read four honest limits before citing this. The key offset is **permanent and load-bearing**: Jira `EOP-184` mirrors GitHub's `EOP-183`, so any bare key in the 182–193 band is ambiguous and only the mirrored description resolves it — the offset exists because GitHub itself spent `EOP-187`, `EOP-188` and `EOP-193` twice each and left one issue unkeyed, so fifteen issues over eleven keys could never correspond one-to-one, and the rejected alternative that would have aligned them collapsed two pairs of distinct work into one ticket each. `main` is **not** rewritten, so `[EOP-167]`, `[EOP-168]` and `[EOP-182]` in the commit log mean the GitHub-keyspace work and always will; that is precisely why those three and only those three were renumbered, the other seven carried-forward keys appearing nowhere in `main`. **Nothing in the build enforces any of this** and no gate is proposed, because the tracker is a live external system rather than a file — the safeguard is that every mirrored and superseded issue states its own provenance, so the record is self-describing rather than checked. And four migration-era GitHub issues (`#290` for `EOP-166`, plus `#285`/`#281`/`#280`) remain open against Jira counterparts that are closed, left untouched deliberately: GitHub is history now, and editing it would imply it is still maintained |
| [067](ADR-067-log-aggregation-with-loki-and-promtail.md) | Container logs are aggregated by Promtail into Loki and read through Grafana, as an opt-in local `compose.observability.yml` stack joined to the pre-existing `eop-monitoring_default` network rather than folded into the k6/InfluxDB performance stack; the application's `prod` JSON encoder is parsed by one pipeline branch and Spring Boot's default plain pattern by a fallback branch, `level` and `logger` become index labels because both are bounded sets, and the per-request `correlationId` is delivered as **structured metadata rather than an index label** because a per-request id as a label is unbounded cardinality — one Loki stream per request (EOP-209) | Accepted | Yes — delivered and verified by query, not by exit code. a `json`-parsing query over `{container=~"eop.*"}` returns real parsed lines across all eight running containers, and a query selecting `{container="eop-app", level="WARN"}` and filtering on `correlationId` returns the provoked WARN **with no `json` parser stage in the pipeline at all**, which is what proves `correlationId` is genuinely structured metadata rather than a parsed field. Read five limits before citing this. It is a **local developer stack only** — nothing in `.github/workflows/` is touched, so no CI behaviour may be claimed of it. Loki runs `auth_enabled: false` and is published on loopback only; that is a deployment fact, not a security control. The `external: true` network name depends on Compose's implicit `<project>_default` convention, so `docker compose -f docker-compose.yml up -d` must already be running or the stack will not start. Two panels the ticket originally contracted — HTTP latency p50/p95 and HTTP status distribution — are **absent because they are unbuildable from current logs**, not descoped by preference: `CorrelationIdFilter` populates MDC but logs nothing and no controller logs a status code or elapsed time, so a request returning 400 with an `x-correlation-id` header produces zero log lines — a standing violation of `.opencode/rules/observability.md` filed as EOP-212. And the two images add to [ADR-064](ADR-064-pinned-container-audit-coverage.md)'s roster, neither carrying a provenance attestation, consistent with the residual already accepted in `SETUP.md` |
| [068](ADR-068-playwright-e2e-testing.md) | End-to-end UI testing with Playwright: a root-level `e2e/` package, a standalone `compose.e2e.yml` that runs the shipped images over TLS, and three browser engines run serially (EOP-216) | Accepted (2026-09-06) | Yes — the scaffold ships green. `globalSetup` brings the stack up with `docker compose --wait` and then polls `https://localhost:8443/health` from the host until it answers 200 with a body of exactly `OK`; one smoke test asserts the home-screen heading renders on Chromium, Firefox and WebKit; `globalTeardown` runs `down -v` so every run starts on an empty database. The tier earns its keep by running the **unmodified** production images, so the shipped Caddy configuration — its security response headers, its request-body cap on `/api/*`, its refusal of source maps, its SPA fallback — is exercised rather than approximated. The cost is that the ticket's original plain-HTTP premise had to be reversed during delivery: no plain-HTTP listener exists in the artefact, and creating one would have meant testing a web server that never ships. Two assumptions were settled empirically rather than argued — Caddy matches its site block on hostname alone, so publishing on host port 8443 needs no configuration change, and `ignoreHTTPSErrors` suffices against `tls internal` provided the target is addressed as `localhost`, because a non-matching SNI is refused server-side. Both rate limiters are raised in the E2E stack, since every browser resolves to one client address and would otherwise share a single bucket, so **this tier must never be cited as evidence that throttling works**. It is deliberately not part of `./mvnw verify` — it needs a container runtime and roughly 500 MB of browser binaries — and is wired into CI by EOP-220 as a post-merge gate rather than a required pull-request check |
| [069](ADR-069-sse-doorbell-catch-up-read.md) | SSE doorbell catch-up read on stream open — the client re-reads state in the SSE stream's `onOpen` callback, closing the race window where an event published before the subscriber is registered is lost (EOP-224) | Accepted (2026-09-06) | Yes — `subscribeToSession` in `ui/src/api.ts` gains an optional fifth parameter `onOpen?: () => void`, invoked once immediately after the response is confirmed `ok` and before the body is read; both call sites (`LobbyScreen.tsx` and `GameScreen.tsx`) pass a catch-up read, so no event published while the stream is being established can be lost. The correctness argument is deterministic: a read issued after the headers observes everything published before the subscriber was registered. Three alternatives were rejected — subscribe-then-read (still has a window), polling on 409 (fixes one symptom, adds timers), and server-side replay via Last-Event-ID (disproportionate — ADR-034 deliberately avoids event history). The `onOpen` parameter is optional in the signature but not in spirit: any future caller that renders server state must pass a catch-up read or reintroduces the defect. A separate, unfixed defect was discovered in the same investigation — requests for a single session block together for up to a full 15-second SSE heartbeat period — and is now resolved by ADR-070. |
| [070](ADR-070-jdbc-connection-leak-via-open-in-view.md) | JDBC connection leak via open-in-view — every SSE stream holds one JDBC connection for its lifetime because `spring.jpa.open-in-view` defaults to true and an async request does not complete until the stream ends (EOP-227) | Accepted (2026-09-06) | Yes — `spring.jpa.open-in-view: false` in `application.yml` removes the root cause; `spring.datasource.hikari.leak-detection-threshold: 10000` adds observability. The fix is two YAML properties, no production Java code changed. `SessionStreamIntegrationTest.shouldNotHoldAJdbcConnectionForTheLifetimeOfAStream` asserts the pool is empty while three streams are open. Three consecutive clean E2E runs (15/15, 59.7s / 59.0s / 58.7s) against 3 failed / 12 passed before the fix. ADR-069's Consequences section is amended to note this resolution. |
| [071](ADR-071-e2e-artefact-publication-boundary.md) | The world-readable page EOP-221 builds is generated from `results.json` alone; the HTML report's residual exposure is accepted, not contained (EOP-228) | Accepted (2026-09-07) | Partly — the parts that exist today are done; the publication step it constrains does not exist yet. The finding as filed was failure-conditional: a trace embeds a player token because `trace`/`screenshot`/`video` fire on failure. Verification against a deliberately failed run confirmed that and then went further — the `html` reporter embeds its whole step tree as a base64 zip in a `<template id="playwrightReportBase64">` element, and decoding the **fully passing** 15/15 baseline recovered **12 distinct live join codes**, each through two channels: a custom `expect()` message that becomes a step title even when the assertion passes, and `locator.fill()` recording the filled value in its own title. So join codes leak on every run and player tokens only on failure, which makes the ticket's "a green run proves nothing" false for the larger of the two channels. A fourth artefact class was also found — `error-context.md`, an aria snapshot carrying the code in plaintext, governed by none of the three settings — and `video` is inert for this suite, since `openSeat` builds contexts by hand and `use.video` governs only the fixture's own. Scrubbing was rejected as unverifiable: the secrets exist in six representations including **visually rendered** screencast JPEGs, and the trace is a version-dependent internal zip whose scrubber would rot silently at the next upgrade. Delivered: `game.ts` reports the join code's length rather than its value, the happy-path reload scenario evaluates a boolean in the browser instead of returning `eop_session`, `e2e/README.md` records the outcome and the decode procedure, and `E2eArtefactPublicationBoundaryTest` fails the build on either source regression or on a workflow step publishing `playwright-report/` to Pages. The `fill()` channel is deliberately left open — closing it would mean not exercising real user input — so the HTML report will always contain join codes. The first draft called the Actions artefact "collaborator-only"; `gh repo view` returns `"visibility":"PUBLIC"`, so on this repository an artefact and a job log are world-readable too and there is no private destination in the pipeline. The ADR was corrected to take EOP-228's *third* option for the residual — an explicit, reasoned acceptance bounded by `down -v` destroying the session before the artefact is downloadable, self-retiring the moment E2E points at a durable environment — and to enumerate the channels it does not close: the `list` reporter's stdout, container and Caddy access logs (the join code is a `{joinCode}` path segment), and `blob-report/`. A fifth build rule was added for a channel the auditor did not raise: a failing matcher prints its received value, so a custom message is not enough and `createSession` now asserts on `joinCode.length` |
| [072](ADR-072-e2e-ci-integration.md) | The end-to-end suite runs after the merge, not before it — a post-merge, nightly and on-demand `e2e` job that never runs on a pull request, so it cannot be made a required check without changing its trigger (EOP-220) | Accepted (2026-09-07) | Yes — `.github/workflows/ci.yml` gains an `e2e` job with `needs: [ image ]` and `if: github.event_name != 'pull_request'`, and the existing `schedule` cron moves from `'0 6 * * 1'` to `'0 6 * * *'` so the whole graph runs nightly rather than weekly. The `image` job gains two steps that `docker save` both `:ci` tags through `gzip` into a `ci-images` artifact, because GHCR cannot be the vehicle — its push steps are gated on `github.event_name == 'push'`, so a nightly or dispatch run would pull the previous commit's `:latest` and test a stale artefact while reporting green. CI owns the stack and sets `E2E_REUSE_STACK=true`, which makes `global-setup.ts` skip its own `up` while still performing the host-side health wait and makes `global-teardown.ts` skip `down -v`, so container logs survive the tests and are collected as their own artifact. Notification is GitHub's own failed-run mechanism with no step producing one, plus a `$GITHUB_STEP_SUMMARY` table naming the scenarios that did not pass first time and `::error::`/`::warning::` annotations that attach to the commit. Four consequences are carried rather than closed: the notification reaches the **merger**, not necessarily the pull-request author, so EOP-220's fifth criterion is met in mechanism but not in its letter, and closing it properly would need the bot the criterion's own rationale ruled out; a regression can sit on `main` for the length of one run, which is what a post-merge detector means; the nightly cron multiplies every scheduled job's runs by seven, so a flake anywhere in the graph is seven times noisier; and `workflow_dispatch` is deliberately **not** restricted to `main`, against the ticket's technical notes, because dispatching from a topic branch is how a change to the suite gets exercised before it merges. The summary reports `file:line`, title, project and status but never assertion text, per ADR-071 — a failing matcher prints its received value, which can be a live join code. `e2e/` remains outside both SonarQube ratchets (EOP-222). |
| [073](ADR-073-supply-chain-coverage-for-actions-and-browsers.md) | Supply-chain audit covers GitHub Actions and Playwright browser binaries | Accepted (2026-09-07) | Yes — three surfaces outside every existing audit are now addressed. GitHub Actions: every `uses:` reference in `.github/workflows/ci.yml` is pinned to a 40-hex SHA with a trailing version comment, nine distinct actions across 29 sites, audited by a new `audit-actions.sh` against `expected-actions.json` — the baseline is a policy, not merely a tripwire, because the audit rejects any bare tag without consulting the baseline. Playwright browsers: the `e2e` job now runs inside the digest-pinned `mcr.microsoft.com/playwright:v1.63.0-noble@sha256:eff16c30…`, eliminating the root `apt-get` and CDN download; it joins `expected-containers.json` as its sixth entry. `e2e/package-lock.json`: the three devDependencies are exact-pinned, and the tree is deliberately not gated — accepted on blast-radius grounds with a retiring condition tied to the `e2e` job gaining secrets or production-data dependencies. `PlaywrightImagePinTest` enforces the image/package-version coupling at build time. |

The "Implemented?" column exists because an accepted ADR is a decision, not a
delivery. `CHANGELOG.md` separates the same two things for the same reason.

## Why numbering starts at 002

There is no ADR-001. The first architectural decision — adopting Spring Boot as the
application framework — was recorded as ADR-002 and 001 was never written. Rather
than renumber a set of documents that other files reference by number, the gap is
recorded here and left alone.

## Why 058 is unused

There is no ADR-058, and the gap is deliberate. The number was allocated to EOP-178, an earlier attempt to close the code-review independence exception by moving `@code-reviewer` onto the existing `MODEL_F`. That work was completed and withdrawn by the operator before merge — PR #305 was closed unmerged and `main` never carried it — because `MODEL_F` already held `@architecture-guardian`, which authors ADRs and C4 models, so the change relocated the same-model-identifier overlap onto architecture documentation instead of eliminating it. [ADR-059](ADR-059-code-review-gate-on-its-own-model-tier.md) records the decision that replaced it. Per step 1 below a number is never reused, even for an abandoned decision, so 058 stays empty and this note is the record — the same treatment the missing 001 gets above.

## Adding an ADR

1. Take the next free number. Do not reuse a number, even for a superseded ADR.
2. Name the file `ADR-NNN-kebab-case-title.md`.
3. Follow the house structure: `# ADR-NNN: Title`, then `**Status:**`, `**Date:**`
   and `**Deciders:**`, then `## Context`, `## Decision`, `## Consequences`,
   `## Related`.
4. State consequences honestly, including the negative ones. An ADR that lists only
   benefits has not recorded a decision, only an advertisement.
5. Add a row to the index above, and keep its Status cell in step with the ADR's own
   `**Status:**` line — including the date whenever an amendment changes the status.
   An amendment that leaves the status word alone, such as withdrawing a consequence
   that was never true, needs no date in either place; that is why several ADRs here
   record a dated amendment — some under a `## Amendments` heading, one as an inline
   `**Amendment, YYYY-MM-DD …**` paragraph — above a bare `Accepted`.
   `AdrIndexConsistencyTest` fails the build if a file has no row, a row points at no
   file, a file carries anything other than exactly one `**Status:**` line, the leading
   status word disagrees, or a dated status line reaches the index without its date.
   It checks that last rule in one direction only — an undated status line is always
   accepted. That test exists because this step was drifted past twice.
6. To reverse a decision, write a new ADR that supersedes the old one and mark the
   old one `Superseded by ADR-NNN`. Do not edit the original's decision.
